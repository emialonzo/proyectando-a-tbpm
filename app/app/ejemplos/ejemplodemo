el usuario tarea a.
si se cumple,
conda es verdadero entonces el usuario tarea b.
condb es verdader entonces no hace nada
si no 
al mismo tiempo,
1 el usuario tarea c.
2 el usuario tarea d.
alternativa de tarea d, se interrumpe si transcurre 1 minuto el usuario tarea e. fin
fin
fin